---
description: Pmax 时间占比分析模块的架构和开发参考
alwaysApply: false
---
# Pmax 时间占比分析模块

## 功能概述

分析轴承最大接触应力（Pmax）的时间占比分布。用户粘贴时间序列和前/后轴承 Pmax 数据，按自定义区间分箱，计算各区间时间占比，生成柱状图。图表可拖拽到 Word。

## 数据流

```
用户粘贴表格数据（持续时间 + Pmax）
→ 前端表格展示
→ 用户设置分箱参数（Min/Max/Step）+ 图形参数
→ 点击「生成图表」→ POST /api/pmax/analyze
→ 后端 pd.DataFrame 解析 → pd.cut 分箱 → groupby 统计占比
→ 计算 min/max 统计摘要 + matplotlib 柱状图
→ PNG 保存到临时目录 → 返回文件路径 + 统计数据
→ 前端 Dialog 展示（local-file:// 协议 + imgCacheBuster 防缓存）
→ 前端显示统计摘要 + 用户可拖拽图片到 Word（Electron startDrag IPC）
```

## 输入数据格式

| 列 | 含义 |
|----|------|
| 持续时间 [h] | 工况持续时间（前轴承） |
| Pmax (MPa)-前 | 前轴承最大接触应力 |
| 持续时间 [h] | 工况持续时间（后轴承） |
| Pmax (MPa)-后 | 后轴承最大接触应力 |

注：表格为 4 列，实际使用列 0、1（前轴承）和列 2、3（后轴承）。

## 参数配置

### 分箱参数（前/后轴承各一组）

| 参数 | 说明 | 默认值（前/后） |
|------|------|----------------|
| Min | 最小区间起点 (MPa) | 800 / 400 |
| Max | 最大区间终点 (MPa) | 1700 / 1200 |
| Step | 区间步长 (MPa) | 100 / 50 |

分箱逻辑：`[-∞, Min), [Min, Min+Step], ..., (Max, +∞]`

### 图表样式参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 标题字号 | 图表标题 | 25 |
| 标签字号 | 坐标轴标签 | 20 |
| 刻度字号 | 刻度标签 | 20 |
| 标注字号 | 柱子上方数值 | 25 |
| 图形宽度 | figsize 宽度 | 20 |
| 图形高度 | figsize 高度 | 15 |

### 图表语言

- 使用全局 `Language` 枚举
- 前端：`constants/language.ts` — `Language.ZH = '中文'` / `Language.EN = 'English'`
- 后端：`common/constants.py` — `Language.ZH = "中文"` / `Language.EN = "English"`
- 前后端值必须保持一致

## 前端文件

- `src/renderer/src/views/PmaxView.vue` — 主页面（无 Pinia store，状态自管理；统计摘要从后端响应获取）
- `src/renderer/src/api/pmax.ts` — 后端 API 调用封装
- `src/renderer/src/constants/language.ts` — 语言枚举（跨模块复用）

## 后端结构

```
backend/
├── common/
│   └── constants.py            # 全局共享常量（Language 枚举等）
├── app_pmax/
│   ├── urls.py                 # 路由注册（POST /api/pmax/analyze）
│   ├── views.py                # 视图逻辑
│   ├── serializers.py          # Pydantic 请求/响应模型
│   └── module/
│       ├── calculator.py       # 分箱计算逻辑
│       └── chart.py            # matplotlib 柱状图生成 → PNG 文件
```

## API 接口

### POST /api/pmax/analyze

接收表格数据和参数，执行分箱计算并生成柱状图。

**请求体（JSON）:**

| 参数 | 类型 | 说明 |
|------|------|------|
| data | `string[][]` | 表格数据，每行 4 列 [持续时间, Pmax前, 持续时间, Pmax后] |
| binConfig | object | `{ front: { min, max, step }, rear: { min, max, step } }` |
| chartConfig | object | `{ titleFontSize, labelFontSize, tickFontSize, textFontSize, width, height }` |
| language | Language | `"中文"` 或 `"English"` |

**响应体（JSON）:**

```json
{
  "front": {
    "chartPath": "/tmp/yanfa7_pmax/front.png",
    "bins": [{ "label": "<800", "percentage": 5.23 }, ...],
    "stats": { "timeMin": 23.01, "timeMax": 2324.94, "pmaxMin": 1050.40, "pmaxMax": 1397.50 }
  },
  "rear": {
    "chartPath": "/tmp/yanfa7_pmax/rear.png",
    "bins": [...],
    "stats": { "timeMin": ..., "timeMax": ..., "pmaxMin": ..., "pmaxMax": ... }
  }
}
```

## 图表文件管理

- PNG 保存到系统临时目录：`os.path.join(tempfile.gettempdir(), "yanfa7_pmax")`
- 固定文件名 `front.png` / `rear.png`，每次生成覆盖写入
- 每次生成前调用 `cleanup_temp_files()` 清理旧文件
- 应用退出时 Electron 主进程同步删除临时目录（`rmSync`）

## 拖拽到 Word

- Electron 渲染进程中 `<img>` 无法直接拖拽到外部应用
- 通过 IPC `drag:start` → 主进程 `webContents.startDrag({ file, icon })` 实现原生文件拖拽
- `icon` 使用 `nativeImage.createFromPath(filePath).resize({ width: 200 })` 生成缩略图

## 核心算法

```python
bin_edges = list(np.arange(min_val, max_val + 1, step))
bin_edges.insert(0, -np.inf)
bin_edges.append(np.inf)

labels = [f"<{bin_edges[1]:.0f}"]
for i in range(1, len(bin_edges) - 2):
    labels.append(f"({bin_edges[i]:.0f}, {bin_edges[i + 1]:.0f}]")
labels.append(f">{bin_edges[-2]:.0f}")

df["bins"] = pd.cut(df["pmax"], bin_edges, include_lowest=False, labels=labels)
result = df.groupby("bins", observed=False)["time"].sum()
result = result / result.sum() * 100  # 时间占比 %
```
