---
description: "[LEGACY/已冻结] Electron IPC/Preload 规范（项目已全面转 Tauri，现行原生能力见 Tauri架构.mdc）"
alwaysApply: false
---

> ⚠️ **LEGACY（已冻结）**：项目已**全面转 Tauri v2**。本文件描述的 Electron IPC / Preload 机制是**待删除的死代码，不代表当前架构**。现行原生能力（兼容垫片 `tauri-bridge.ts`、`invoke`、`#[tauri::command]`）见 `Tauri架构.mdc`。

# Electron IPC 规范（legacy）

## Preload 脚本
- 通过 `contextBridge.exposeInMainWorld('electronAPI', ...)` 暴露自定义 API
- `@electron-toolkit/preload` 的 API 通过 `window.electron` 访问
- 自定义 API 通过 `window.electronAPI` 访问
- 类型声明在 `src/preload/index.d.ts`

## IPC 命名规范
```
dialog:openFile       # 文件选择对话框
dialog:openDir        # 目录选择对话框（仅选择目录，返回路径）
dialog:openDirectory  # 目录选择对话框（通用）
fs:scanDir            # 递归扫描目录中的指定扩展名文件
window:openNew        # 打开新窗口
drag:start            # 原生文件拖拽（ipcMain.on，非 handle）
```

### 文件扫描两步模式
选择目录并扫描文件拆为两个 IPC，以便前端在扫描期间显示 loading 状态：
1. `dialog:openDir` — 弹出系统目录选择对话框，返回 `{ canceled, rootDir }`
2. `fs:scanDir` — 接收 `{ rootDir, extensions }`, 递归扫描并返回 `{ filePaths, fileSizes }`

## 安全原则
- 不启用 `nodeIntegration`
- 使用 `sandbox: false` 仅因预加载脚本需要
- 所有 IPC 调用都经过 preload 中转，不直接暴露 `ipcRenderer`
