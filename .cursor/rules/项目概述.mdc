---
alwaysApply: true
---

# 研发七部工具包 项目概述

这是一个**轴承数据分析桌面工具包**，前后端分离架构，当前包含「边缘应力分析」模块，后续可扩展更多工具。

## 技术栈
- **前端**: Tauri v2（Rust 外壳）+ Vue 3 + TypeScript + Element Plus + Pinia
- **构建**: Tauri CLI + Vite（渲染层）
- **后端**: Python (FastAPI)，独立 sidecar 进程（HTTP/WS :8000）

> 项目曾是 Electron + electron-vite，**现已全面转 Tauri v2**。`src/main`、`src/preload`、`electron.vite.config.ts` 及 `electron*` 依赖是**冻结的 legacy 死代码**（迁移收尾后删除），运行时不再加载。Tauri 端架构详见 `Tauri架构.mdc`。

## 项目结构
```
yanfa7/
├── frontend/
│   ├── src-tauri/     # Tauri Rust 外壳（main.rs/lib.rs、tauri.conf.json、capabilities/、icons/）
│   ├── src/
│   │   ├── renderer/  # Vue 渲染进程（实际前端）
│   │   │   └── src/
│   │   │       ├── views/       # 页面组件
│   │   │       ├── stores/      # Pinia 状态管理
│   │   │       ├── api/         # 后端 HTTP/WS 接口（:8000）
│   │   │       ├── router/      # Vue Router
│   │   │       ├── composables/ # Vue Composables
│   │   │       ├── components/  # 通用组件
│   │   │       ├── shims/       # tauri-bridge.ts：重建 window.electron* 兼容层
│   │   │       └── assets/      # 静态资源（logo、图标等）
│   │   ├── main/      # ⚠️ legacy：Electron 主进程（冻结死代码，待删）
│   │   └── preload/   # ⚠️ legacy：Electron 预加载（冻结；仅 index.d.ts 类型声明仍被引用）
└── backend/           # Python 后端服务（FastAPI sidecar）
```

## 关键约定
- 路径别名: `@renderer` (映射到 `src/renderer/src/`)、`@root` (映射到项目根目录 `frontend/`)
- 前后端通信: 渲染进程 → HTTP/WS API → Python 后端（:8000）
- **原生能力**: 渲染层一律调 `window.electronAPI.*` / `window.electron.*`，由 `src/renderer/src/shims/tauri-bridge.ts` 兼容垫片翻译成 Tauri 调用（`invoke`、`@tauri-apps/plugin-*`、`WebviewWindow`）。新增能力 = 在 bridge 加方法（必要时在 `src-tauri/src/lib.rs` 加 `#[tauri::command]` 并更新 `generate_handler!` 与 `capabilities/default.json`）。详见 `Tauri架构.mdc`
- 设置持久化: 使用 `@tauri-apps/plugin-store`（`settings.json`，`autoSave`），在 bridge 中懒加载单例
- 窗口标题: 研发七部工具包
- 静态资源（logo、SVG 图标等）放在 `src/renderer/src/assets/` 目录
- `resources/` 目录仅用于存放打包时的后端产物（`resources/backend/`）
- 侧边栏底部菜单项通过路由 meta `bottomNav: true` 标记，与主菜单项区分

## 暗色模式
- 主题切换走兼容垫片 `tauri-bridge.ts` 的 `applyTheme(mode)`: ① `getCurrentWindow().setTheme(mode==='system'?null:mode)` 驱动原生窗口与 webview 的 `prefers-color-scheme`；② `document.documentElement.classList.toggle('dark', ...)`
- 主题持久化在 `@tauri-apps/plugin-store`（`settings.json` 的 `appearance` 键）；启动时 `initTauriBridge()` 读取并 `applyTheme`
- `system` 模式回落到 `window.matchMedia('(prefers-color-scheme: dark)')`；`App.vue` / `preview.ts` 各挂 matchMedia 监听跟随系统 toggle `.dark` class
- Element Plus 组件通过 `element-plus/theme-chalk/dark/css-vars.css` 自动适配暗色
- **自定义样式的暗色覆盖**统一写在 `src/renderer/src/assets/main.css` 中，使用 `html.dark .xxx` 选择器
- 新增页面/组件时，若含硬编码颜色值，必须在 `main.css` 中添加对应的 `html.dark` 覆盖样式
- 暗色模式色板参考: 背景 `#1c1c1e`/`#2c2c2e`/`#3a3a3c`，文字 `#f5f5f7`/`#a1a1a6`/`#636366`，边框 `#38383a`/`#48484a`
- 设置入口 `SettingsView.vue` 调 `window.electronAPI.theme.set/get`；需 `core:window:allow-set-theme` 权限（`capabilities/default.json`）

## 文档约定
- **`yanfa7/README.md`**（项目根目录）: 面向用户的使用说明（功能介绍、使用步骤、快捷键等），会在应用内「文档」页面中通过 `@root/README.md?raw` 导入并渲染展示
- **`.cursor/rules/`**: 面向开发者的技术规范和架构说明（项目结构、组件规范、开发约定等）
- 对话结束时，面向用户的改动更新到根目录 README.md，面向开发者的改动更新到对应的 rule 文件
- 前端/后端子目录不再各自维护 README.md

## 对话流程规则
- **对话开始时**: 阅读相关的 `.cursor/rules/` rule 文件了解项目架构和开发约定
- **对话结束时**: 当用户表示可以结束对话，除了输出改动总结和 git 提交命令外，还必须同步更新根目录 `README.md`（面向用户的使用说明）和对应的 rule 文件（面向开发者的技术规范），确保两类文档各自反映最新状态
